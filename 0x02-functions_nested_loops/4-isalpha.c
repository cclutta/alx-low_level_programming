#include <stdlib.h>
#include <stdio.h>



/**
 * _isalpha- its main
 * @c: bitch
 *
 * Return: 0
 */
int _islower(int c)
{
if ((c >= 'a' && c <= 'z') && (c >= 'A' && c <= 'Z'))
{
return (1);
}
else
{
return (0);
}


}
