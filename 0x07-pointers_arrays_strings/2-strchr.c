#include "main.h"

/**
* _strchr - that locates a character in a string.
* @s: pointer
* @c: char
*
* Return: char pointer
*/

char *_strchr(char *s, char c)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		if (s[i] == c)
		
		return (s);
	}
	return (s);
}
